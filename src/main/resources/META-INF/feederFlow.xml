<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:int-http="http://www.springframework.org/schema/integration/http"
	xmlns:int-file="http://www.springframework.org/schema/integration/file"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/integration/http http://www.springframework.org/schema/integration/http/spring-integration-http.xsd
		http://www.springframework.org/schema/integration/file http://www.springframework.org/schema/integration/file/spring-integration-file.xsd">


	<int:poller id="poller"  default="true" fixed-rate="300000"></int:poller>

	<!-- HTTP -->	
	<int-http:outbound-gateway id="quotationsHttpGateway"
		request-channel="trigger.channel"
		url="https://docs.google.com/spreadsheets/d/16CrPHjsp8MBXfK8hEYsSDQHHHp_kFCi8vuidwTv02z0/pub?output=csv"
		http-method="GET" expected-response-type="java.lang.String" charset="UTF-8"
		reply-timeout="5000" reply-channel="quotations.channel">
	</int-http:outbound-gateway>
	
	<int:inbound-channel-adapter channel="trigger.channel"
		expression="''" id="httpIn">	</int:inbound-channel-adapter>
		
	<!-- Input Files -->
	<int-file:inbound-channel-adapter id="historicalFilesIn"
		prevent-duplicates="true" channel="historical.channel"
		directory="${historical.files.input.directory}">
    	<int:poller fixed-rate="5000"></int:poller>
    </int-file:inbound-channel-adapter>

	<int-file:file-to-string-transformer input-channel="historical.channel" output-channel="quotations.channel"/>
	
	<!-- Splitter, converter & publisher-->
	<int:chain input-channel="quotations.channel" output-channel="quotationsPublishChannel">
		<int:splitter ref="CSVLineSplitter" ></int:splitter>
		<int:service-activator
			expression="@CSVStockQuotationConverter.converToStockQuotation(payload)">
		</int:service-activator>
	</int:chain>
	
	<int:publish-subscribe-channel id="quotationsPublishChannel"></int:publish-subscribe-channel>

	<!--  Consumers -->	
	<int:service-activator input-channel="quotationsPublishChannel"
		expression="@ELKClient.pushToELK(payload)" output-channel="nullChannel">
	</int:service-activator>
	
	<int:logging-channel-adapter channel="quotationsPublishChannel"
		expression="payload">
	</int:logging-channel-adapter>
	
	
	<!-- Channels -->
	<int:channel id="quotations.channel"></int:channel>
	<int:channel id="trigger.channel"></int:channel>
	<int:channel id="historical.channel"></int:channel>
	
</beans>